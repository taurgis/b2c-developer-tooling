*-debug.log
*-error.log
**/.DS_Store
/.idea
/.codex/
dist
dist-deploy
/tmp
node_modules
/coverage
coverage/
oclif.manifest.json
*.tsbuildinfo


yarn.lock
package-lock.json


.claude/settings.local.json
.envrc

# VitePress
docs/.vitepress/dist
docs/.vitepress/cache

# TypeDoc generated API docs (regenerated on build)
docs/api/

# Salesforce Help corpus: the converted .md tree is packed into the committed
# docs/help-content.tar.gz and extracted into the docs build output at build time
# (VitePress buildEnd). The generator's staging tree is transient, not committed.
packages/b2c-tooling-sdk/tmp/

*dw.json*
!packages/b2c-vs-extension/src/test/fixtures/**/dw.json
.env
.config/wt.toml
.b2c/

# Eval project has its own git repo for isolation from the main project
skills/eval/project/.git
# Eval results
skills/eval/results-*.json
# Python bytecode
__pycache__/
